import React from 'react'
import ReactDOM from 'react-dom/client'
import SorteoGrupos from './SorteoGrupos.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SorteoGrupos />
  </React.StrictMode>,
)
